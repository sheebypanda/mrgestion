<p id="notice"><%= notice %></p>

<table class="table table-hover table-borderless">
  <thead>
    <tr>
      <th>Date</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @jourferies.each do |jourfery| %>
      <tr>
        <td><%= l(jourfery.date, format: '%A %d %B %Y') %></td>
        <td><%= link_to 'Modifier', edit_jourfery_path(jourfery) %></td>
        <td><%= link_to 'Détruire', jourfery, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
